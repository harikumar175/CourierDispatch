<?php
session_start();
if(isset($_POST['submit']))
{
  include'dbh.inc.php';
  $uid=mysqli_real_escape_string($conn,$_POST['username']);
  $pwd=mysqli_real_escape_string($conn,$_POST['password']);
  if(empty($uid)||empty($pwd))
  {
    header("location:branch1signin.php?login=empty");
    exit();
  }
  else
  {
    $sql="SELECT*FROM branch1user WHERE username='$uid'";
    $result=mysqli_query($conn,$sql);
    $resultCheck=mysqli_num_rows($result);
    if($resultCheck<1)
    {
      header("location:branch1signin.php?login=error");
      exit();
    }
    else {
      if($row=mysqli_fetch_assoc($result))
      {
        $passwordCheck=strcmp($pwd,$row['pwd']);

        if($passwordCheck==0)
        {
          $SESSION['username']=$row['username'];
          header("location:branch1admin.php");
          exit();
        }
        else
        {
          header("location:branch1signin.php?login=error");
          exit();
        }
      }
    }
  }
}
 ?>
